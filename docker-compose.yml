
# Định nghĩa mạng nội bộ (Phòng họp kín)
networks:
  minicloud-network:
    driver: bridge

# Định nghĩa các Nhạc công (Services)
services:

  # 1. Nhạc công Database (PostgreSQL)
  db:
    image: postgres:16-alpine
    # container_name: minicloud-db-compose
    environment:
      # Đọc từ file .env (${TEN_BIEN})
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}

    healthcheck:
      # Lệnh kiểm tra: "Này Postgres, mày sẵn sàng chưa?"
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s        # Cứ 5 giây hỏi 1 lần
      timeout: 5s         # Chờ trả lời tối đa 5 giây
      retries: 5          # Thử 5 lần không được mới báo là "Unhealthy" (Ốm)
    # Quan trọng: Tạo ổ cứng bền vững để không mất dữ liệu khi tắt
    volumes:
      # Để Docker tự quản lý volume (An toàn nhất trên WSL)
      - postgres_data:/var/lib/postgresql/data 
    networks:
      - minicloud-network # Cho vào phòng họp
    restart: always
  
  # 2. Nhạc công API (Backend .NET)
  api:
    # container_name: minicloud-api-compose
    # Ở file gốc, ta chưa nói Build bằng cách nào, cũng chưa mở Port

    # API phải đợi DB chạy xong mới được chạy
    depends_on:
      db:
        condition: service_healthy # Chỉ khi DB khỏe mạnh mới cho API chạy
    networks:
      - minicloud-network # Cho vào phòng họp cùng DB
    restart: always # Đổi từ on-failure sang always cho "trâu bò" hơn

  # 3. Nhạc công MinIO (Lưu trữ tệp)
  minio:
    image: minio/minio:latest
    container_name: minicloud-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio_data:/data # Tạo volume để lưu trữ dữ liệu
    networks:
      - minicloud-network
    restart: always

  redis:
    image: redis:alpine
    container_name: minicloud-redis
    command: redis-server --appendonly yes # Giúp lưu dữ liệu xuống ổ cứng để không mất khi restart
    volumes:
      - redis_data:/data 
    networks:
      - minicloud-network
    restart: always

  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: minicloud-redis-ui
    environment:
      - REDIS_HOSTS=local:redis:6379 # Kết nối vào container 'redis'
    ports:
      - "8081:8081" # Cổng Web quản lý
    networks:
      - minicloud-network
    depends_on:
      - redis
  seq:
    image: datalust/seq:latest
    container_name: minicloud-seq
    environment:
      - ACCEPT_EULA=Y # Đồng ý điều khiển sử dụng
      - SEQ_FIRSTRUN_ADMINPASSWORD=${SEQ_PASSWORD}
    ports:
      - "5341:5341" # Cổng nhận dữ liệu
      - "8088:80" # Cổng Web UI (Map từ 80 trong container ra 8088 ngoài)
    networks:
      - minicloud-network
    restart: always
# Khai báo volume ở cuối file
volumes:
  postgres_data:
  minio_data: # Tạo volume cho MinIO
  redis_data: # Tạo volume cho Redis

